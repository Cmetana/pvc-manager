# PVC Manager — Система керування дільницею

## 🚀 Швидкий старт (покрокова інструкція)

### 1️⃣ Передумови
Переконайтесь що встановлено:
- **Node.js v18+**: `node --version`
- **Docker Desktop**: [Завантажити](https://www.docker.com/products/docker-desktop/)
- **Git**: `git --version`

---

### 2️⃣ Отримання Telegram Bot Token

1. Відкрийте Telegram → знайдіть **@BotFather**
2. Надішліть `/newbot`
3. Введіть назву бота (наприклад: `PVC Manager`)
4. Введіть username бота (наприклад: `pvc_manager_bot`)
5. Скопіюйте **токен** — він виглядає як `1234567890:ABC-DEF...`

---

### 3️⃣ Налаштування проекту

```bash
# Клонуйте або скопіюйте папку проекту в зручне місце
cd ~/Projects
cp -r pvc-manager/ mій-pvc-manager/
cd мій-pvc-manager

# Встановіть залежності
npm install

# Скопіюйте .env і заповніть
cp .env.example .env
```

Відкрийте `.env` у VS Code і заповніть:
```env
DATABASE_URL="postgresql://pvc_user:pvc_pass@localhost:5432/pvc_db"
BOT_TOKEN="ваш_токен_від_BotFather"
API_PORT=3000
WEBAPP_URL="http://localhost:5173"
ADMIN_URL="http://localhost:5174"
```

---

### 4️⃣ Запуск бази даних

```bash
# Запускаємо PostgreSQL через Docker
docker-compose up -d

# Перевіряємо що контейнер запустився
docker ps
# Має бути: pvc_postgres ... Up
```

---

### 5️⃣ Ініціалізація бази даних

```bash
# Генеруємо Prisma Client
npx prisma generate

# Синхронізуємо схему з БД
npx prisma db push

# Заповнюємо тестовими даними
npm run db:seed
```

---

### 6️⃣ Встановлення залежностей WebApp та Адмінки

```bash
# Встановити всі залежності одразу
npm run install:all
```

---

### 7️⃣ Запуск всього проекту

```bash
# Запускає: Bot + API + WebApp + Адмінка
npm run dev
```

Ви побачите:
```
✅ Підключено до БД
🤖 Бот @ваш_бот запущено (polling)
🚀 API запущено на http://localhost:3000
⏰ Крон-задачі запущено
```

---

### 8️⃣ Перевірка

| Сервіс | URL | Що має бути |
|--------|-----|-------------|
| **API** | http://localhost:3000/health | `{"status":"ok"}` |
| **WebApp** | http://localhost:5173 | Інтерфейс працівника |
| **Адмінка** | http://localhost:5174 | Форма входу |
| **Бот** | Telegram → /start | Вітальне повідомлення |

> **Перший /start** → бот автоматично призначить вас Адміном

**Вхід в адмінку:**
1. Відкрийте http://localhost:5174
2. Введіть ваш Telegram ID (дізнатись: @userinfobot в Telegram)
3. Натисніть "Увійти"

**WebApp в Telegram:**
1. Встановіть ngrok: `brew install ngrok/ngrok/ngrok`
2. Зареєструйтесь на ngrok.com → скопіюйте authtoken
3. `ngrok config add-authtoken ВАШ_ТОКЕН`
4. `ngrok http 5173`
5. Скопіюйте URL (https://xxx.ngrok.io) → вставте в `.env` як `WEBAPP_URL`
6. Перезапустіть бот

---

## 📱 Команди бота

| Команда | Дія |
|---------|-----|
| `/start` | Реєстрація / головне меню |
| `/menu` | Показати меню |
| `/stats` | Звіт план/факт (тільки адмін) |
| `/users` | Список користувачів (тільки адмін) |
| `/pending` | Лист очікування (тільки адмін) |
| `/help` | Форма запиту допомоги |

### Команди в чаті:
- `переробка:123:причина` — відправити задачу на переробку
- `причина:123:текст` — прострочена задача (коментар)
- `допомога: текст` — запит допомоги
- `допомога:#123: текст` — допомога прив'язана до задачі

---

## 🔧 Корисні команди розробки

```bash
# Перегляд БД у браузері
npm run db:studio

# Скинути і перезаповнити БД
npx prisma db push --force-reset && npm run db:seed

# Дивитись логи Docker
docker logs pvc_postgres -f
```

---

## 📁 Структура проекту

```
pvc-manager/
├── src/
│   ├── bot/                  ← Telegram Bot
│   │   ├── index.ts          ← Точка входу бота
│   │   ├── cron.ts           ← Крон-задачі
│   │   └── handlers/
│   │       ├── onboarding.ts ← /start, реєстрація
│   │       ├── tasks.ts      ← Дії з задачами
│   │       ├── help.ts       ← Запити допомоги
│   │       └── admin.ts      ← Адмін команди
│   ├── api/                  ← REST API
│   │   ├── index.ts          ← Fastify сервер
│   │   └── routes/
│   │       ├── tasks.ts      ← /api/tasks
│   │       ├── users.ts      ← /api/users
│   │       └── stats.ts      ← /api/stats, /api/refs
│   ├── db/
│   │   └── client.ts         ← Prisma Client
│   └── shared/
│       └── constants.ts      ← Утиліти, константи
├── prisma/
│   ├── schema.prisma         ← Схема БД
│   └── seed.ts               ← Тестові дані
├── docker-compose.yml        ← PostgreSQL
├── .env.example              ← Шаблон змінних
└── package.json
```

---

## ✅ Повний список реалізованого функціоналу

### Telegram Bot
- `/start` — реєстрація, перший юзер → автоматично адмін
- Онбординг: кнопки Адмін / Працівник / Бан прямо в чаті
- Взяти задачу в роботу, виконати, переробка з коментарем
- Підтвердження переробки адміном через inline кнопку
- Прострочені задачі — запит коментаря прямо в чаті
- Запит "Допомога" — `допомога: текст` або `допомога:#123: текст`
- `/stats` `/users` `/pending` для адміна
- Крон: звіт план/факт о 08:00/11:00/14:00/17:00/20:00
- Крон: прострочені задачі щодня о 10:00

### REST API
- CRUD задач з автопризначенням підкоманди по компетенції
- Зміна статусів з валідацією переходів
- Масова зміна планової дати
- Статистика план/факт по днях, типах, працівниках
- Довідники: типи, компетенції, підкоманди
- Імпорт Google Sheets (прев'ю + виконання)
- Нерозподілені задачі + ручне призначення бригади
- Запити допомоги (список + закриття)

### WebApp (Telegram)
- 📋 Пул задач підкоманди з фільтрами (дата, статус, тип)
- 🔧 Мої задачі — таби В роботі / Переробка / Виконано
- 📊 Статистика — план/факт, по типах, hours_per_SP

### Адмінка (браузер)
- 📋 Задачі — таблиця, фільтри, форма, масова зміна дати, підтвердження переробки
- 🔴 Нерозподілені — задачі без бригади + ручне призначення
- 👥 Користувачі — лист очікування, ролі, бригади, компетенції
- 📚 Довідники — типи / компетенції / бригади (CRUD)
- 📥 Імпорт Google Sheets — маппінг, прев'ю, режими додати/оновити
- 🆘 Допомога — список запитів, закриття
- 📊 Статистика — по днях / по працівниках / по типах + **Експорт CSV та XLSX**
